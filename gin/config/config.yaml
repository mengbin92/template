server:
  port: 8080
  mode: debug

log:
  # 支持debug(-1)、info(0)、warn(1)、error(2)、dpanic(3)、panic(4)、fatal(5)
  level: 0
  # 日志输出格式，支持json or console
  format: console

database:
    driver: mysql
    source: root:Hvrg(CPVYUf^V+Qv@tcp(127.0.0.1:3306)/example?charset=utf8mb4&parseTime=True&loc=Local