# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Demo API
    description: Demo service provides example API endpoints
    version: 0.0.1
paths:
    /demo/health:
        get:
            tags:
                - Demo
            description: Health check endpoint
            operationId: Demo_CheckHealthy
            parameters:
                - name: service
                  in: query
                  description: 'Optional: service name to check'
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.demo.v1.CheckHealthyResponse'
    /demo/hello:
        get:
            tags:
                - Demo
            description: GetHello returns a hello message
            operationId: Demo_GetHello
            parameters:
                - name: name
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.demo.v1.Reply'
components:
    schemas:
        api.demo.v1.CheckHealthyResponse:
            type: object
            properties:
                status:
                    type: string
                    description: 'Health status: "healthy", "degraded", "unhealthy"'
                service:
                    type: string
                    description: Service name
                timestamp:
                    type: string
                    description: Timestamp of the health check
                details:
                    type: object
                    additionalProperties:
                        $ref: '#/components/schemas/api.demo.v1.HealthDetails'
                    description: Health details for each component (database, redis, etc.)
            description: CheckHealthyResponse contains the health status of the service
        api.demo.v1.HealthDetails:
            type: object
            properties:
                status:
                    type: string
                    description: 'Component status: "healthy", "unhealthy", "unavailable", "not_configured"'
                error:
                    type: string
                    description: Optional error message if component is unhealthy
                latencyMs:
                    type: number
                    description: Optional latency in milliseconds
                    format: double
            description: HealthDetails contains detailed health information for each component
        api.demo.v1.Reply:
            type: object
            properties:
                code:
                    type: integer
                    description: Response status code, 0 indicates success, non-zero indicates failure
                    format: int32
                message:
                    type: string
                    description: Response message
                data:
                    type: object
                    description: Response data (using Struct type, can directly return JSON object)
            description: Common response structure
tags:
    - name: Demo
